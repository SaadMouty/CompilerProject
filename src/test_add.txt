<div class="form-container">
  <h2>إضافة منتج جديد للمتجر</h2>

  {% if user_is_admin %}
    <form action="/add" method="POST">
      <input type="text" name="product_name" placeholder="اسم المنتج" />
      <input type="number" name="product_price" placeholder="السعر" />
      <textarea name="details">أضف تفاصيل المنتج هنا</textarea>
      <button type="submit">حفظ المنتج</button>
    </form>
  {% else %}
    <p>عذراً، ليس لديك صلاحية لإضافة منتجات.</p>
  {% endif %}
</div>